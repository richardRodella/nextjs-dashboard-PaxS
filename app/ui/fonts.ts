import { Inter } from 'next/font/google';
import { Roboto_Serif } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const robotoSerif = Roboto_Serif({ subsets: ['latin'], display: 'swap', weight: ['400','600'] });